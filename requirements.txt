# Direct dependencies of the code in this repository (Python >= 3.11).
# For GPU training, you may first install the PyTorch build that matches your
# CUDA version (https://pytorch.org/get-started/locally/).
torch==2.10.0
transformers==5.0.0
datasets==4.0.0
numpy==2.0.2
scikit-learn==1.6.1
scipy==1.16.3
torchmetrics==1.9.0
tqdm==4.67.3
